class Cylinder2 extends CGFobject {
  constructor(scene,base,top,hgt,slices,stacks) {
    super(scene);
    this.base = base;
    this.top = top;
    this.height = hgt;
    this.slices = slices;
    this.stacks = stacks;
  }
}